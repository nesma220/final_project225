import 'package:flutter/material.dart';

class CalendarToday extends StatelessWidget {
  const CalendarToday({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}